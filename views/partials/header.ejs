<!DOCTYPE html>
<html>
  <head>
    <title>Role camp</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
    <link rel="stylesheet" href="/stylesheets/campgrounds.css">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css">
  </head>
  
  <body>
    <nav class="navbar navbar-inverse">
      <div class="container-fluid">
        <div class="container">
          <div class="navbar-header">
            <a class="navbar-brand white-text" href="/campgrounds"> Role Camp </a>
          </div>
          <div class="nav navbar-collapse">
            <ul class="nav navbar-nav navbar-right">
              <% if (!currentUser) { %>
                <li><a class="white-text" href="/login"> Login </a></li>
                <li><a class="white-text" href="/register"> Sign In</a></li>
              <% } else { %>
                <li><a>Welcome, <%= currentUser.username %></a></li>
                <li><a class="white-text" href="/logout"> Logout </a></li>
              <% } %>
            </ul>
          </div>
        </div>
      </div>
    </nav>
    <div class="container">
      <% if (error && error.length > 0) { %>
        <h3 class="alert alert-danger"><%= error %></h3>
      <% } %>
      
      <% if (success && success.length > 0) { %>
        <h3 class="alert alert-success"><%= success %></h3>
      <% } %>
    </div>